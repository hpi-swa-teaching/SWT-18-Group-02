An ACMenuMorph is a menu displayed while typing, which shows aurocorrection-suggestions. ACMenuMorph is the view in a MVC pattern.
Instance Variables
	model:		<Object>

model
	- is the model in the MVC pattern
